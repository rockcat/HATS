/**
 * Standalone UI demo — shows the dashboard with a mock orchestrator.
 *
 * Usage:
 *   node --env-file=.env --import tsx/esm webui-demo.ts
 */

// The dashboard is now served directly by team-demo.ts via the APIServer.
// Run team-demo.ts to get the full live experience.
//
// This file is kept as a placeholder — run the real thing:
console.log('The dashboard is part of the main team server.');
console.log('Run: node --env-file=.env --import tsx/esm team-demo.ts');
console.log('Then open: http://localhost:3001');
